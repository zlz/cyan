const path = require('path');
const fs = require('fs');
const dataFile = path.resolve('./src/datas/album.json');
const data = fs.readFileSync(dataFile);
const album = JSON.parse(data);
let lang = $_GET['lang'];
let res = {};
if (album[$_GET['lang']]) {
    res = album[$_GET['lang']]
}
console.log(__env.REQURL);
echo({
    code: 200,
    header: $_HEADER,
    location: '',
    msg: 'success',
    data: res
});
end();